# Aliases (DEPRECATED)

> [!WARNING]
> These bash aliases are now deprecated in favor of the [Lab CLI](./12-lab-cli.md). 
> It is recommended to use `lab` for all operations.

The aliases file gives you short commands for daily hosting work.

File:

```text
scripts/aliases.sh
```

## Enable For Current Terminal

```bash
source /mnt/storage/arch-vps-server/scripts/aliases.sh
```

Then check available commands:

```bash
avps-help
```

... (rest of the content)
